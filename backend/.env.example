# Example environment configuration for MiniCord backend
# Copy to `.env` and adjust values for your environment.

PORT=3000
NODE_ENV=development
# Set this to your frontend origin (no trailing slash)
FRONTEND_ORIGIN=http://localhost:5173
