$kind: http-request
name: Login
order: 2000
method: POST
url: '{{baseUrl}}/api/auth/login'
auth:
  type: noauth
description: Login with email and password. Returns user info and a JWT token.
headers:
  - key: Content-Type
    value: application/json
body:
  type: json
  content: |-
    {
      "email": "testuser@example.com",
      "password": "password123"
    }
scripts:
  - type: afterResponse
    language: text/javascript
    code: |-
      const res = pm.response.json();
      if (res.token) {
        pm.collectionVariables.set("authToken", res.token);
      }
